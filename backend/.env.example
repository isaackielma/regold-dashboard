# ─────────────────────────────────────────────────────────────────────────────
# ReGold Backend — Environment Variables
#
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# Use a secrets manager (AWS Secrets Manager, Doppler, 1Password) in production.
# ─────────────────────────────────────────────────────────────────────────────

# Server
PORT=5001
NODE_ENV=development                  # development | production | test

# Database
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/regold_db
# For production use connection pooler URL (e.g. PgBouncer / Supabase pooler)
# DATABASE_URL=postgresql://user:pass@db.example.com:6543/regold_db?pgbouncer=true

# Auth — generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace_with_64_byte_random_hex_string
JWT_EXPIRY=7d                         # token lifetime

# Email (for verification emails) — uses nodemailer under the hood
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your_sendgrid_api_key
EMAIL_FROM=noreply@rebijoux.com

# XRPL
XRPL_NETWORK=testnet                  # testnet | mainnet
XRPL_SERVER_URL=wss://s.altnet.rippletest.net:51233
XRPL_MPT_ISSUER=                      # fill when you have the issuer address
XRPL_MPT_CURRENCY=ReGold

# CORS — comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000
